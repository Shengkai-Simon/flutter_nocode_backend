server:
  port: ${SERVER_PORT:9000}

spring:
  application:
    name: ${APP_NAME:auth-service}

  datasource:
    url: ${POSTGRE_SQL:jdbc:postgresql://localhost:5432}/authdb
    username: user
    password: pass

  security:
    oauth2:
      authorizationserver:
        issuer: ${JWT_ISSUER_URI:http://localhost:9000}