spring:
  security:
    oauth2:
      authorizationserver:
        issuer: http://localhost:9000

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
  data:
    redis:
      host: localhost
      port: 6379

app:
  security:
    internal-api-key:
      primary: test-internal-api-key